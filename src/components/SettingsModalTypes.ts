export interface ArenaSettings {
  minimapPosition: "left" | "right";
  minimapScale: "1" | "1.2" | "1.5";
  showInterface: boolean;
  showOverlay: boolean;
}
export interface ControlSettings {
  handedness: "left" | "right";
  controlScheme: "buttons" | "pointer" | "joystick" | "drag";
}
